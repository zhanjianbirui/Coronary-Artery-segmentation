# 模型权重 / checkpoint
*.pth
*.pt
*.ckpt

# 训练输出目录
# ⚠️ 不要写 `runs/` 或 `**/runs/` —— 那会排除【目录本身】，Git 从此不再进入该目录，
#    里面任何 `!` 白名单规则都失效（BUG-008）。论文引用的评估 csv 就是这样被漏掉的：
#    2026-08-28 发现 stage2 三个变体 + 三正交 v2 基线四个 csv 从未入库，
#    而已被 EXP-014 作废的 v1 基线反而在库里。
#    因此这里按【文件类型】排除过程产物，目录保持可进入，csv 得以入库。
runs/**/*.out
runs/**/*.err
runs/**/*.log
runs/**/*.yaml
runs/**/*.npz
runs/**/*.nii.gz

# 数据和缓存
cache/
*.nii
*.nii.gz
ImageCAS/

# Python 缓存
__pycache__/
*.pyc
.ipynb_checkpoints/

# 可视化输出
vis_cases/
debug_vis/
*.png

# 系统文件
.DS_Store
